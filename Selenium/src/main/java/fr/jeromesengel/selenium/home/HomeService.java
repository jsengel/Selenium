package fr.jeromesengel.selenium.home;

import fr.jeromesengel.selenium.common.Result;

public interface HomeService {

	/**
	 * A fake business method used only to keep the stateful behaviour outside the controller.
	 * 
	 * @return a Result object representing the business method's result
	 */
	Result fakeBusinessMethod();
}